You are updating a long-term learner trajectory dashboard JSON for a single user.

Return ONLY strict JSON (no markdown, no commentary).

Hard constraints:
- Do not use praise, hype, or sycophantic language.
- Make claims only when supported by checkpoints.
- Prefer stable, sparse updates over noisy ones.
- Never delete existing highlights; only append if truly meaningful.
- Every highlight must include: kind, summary, confidence (0..1), and evidence_quote when available.

Output schema (must match):
{{
  "version": 1,
  "user_id": "<string>",
  "updated_at": "<ISO timestamp or null>",
  "insights": "<string: 2-3 sentences about underlying patterns connecting all interests/goals>",
  "highlights": [ ... ],
  "goals": [ ... ],
  "engagement": {{ ... }},
  "learner_model": {{ ... }}
}}

What to produce:

1. **insights** (REQUIRED - cross-cutting observations):
   - Look at the "themes" array in the context data
   - Identify what underlying pattern or core interest connects these themes
   - Examples: "Interests center on understanding mechanisms rather than applications", "Drawn to mathematical foundations and optimization", "Focuses on theory before practice"
   - Reference their curiosity type, entry mode, or other dimensions from learner_model if it explains the pattern
   - Write 2-3 specific, factual sentences
   - NEVER leave this empty - always generate something based on available data

2. **goals** list:
   - IMPORTANT: Use field name "goal_id" (NOT "id") for the goal identifier
   - CRITICAL: If context.goals array contains goals, you MUST include ALL of them in your output
   - Extract goals from context.goals array if available
   - Schema: {{"goal_id": int, "goal_text": str, "status": str, "momentum": str, "last_activity_at": str|null, "learning_summary": str, "next_suggested_move": str}}
   - For each goal in context.goals: populate goal_id from context.goals[].goal_id, set status (active/paused), momentum (moving/stuck/inactive based on recent activity)
   - If context.goals has goals but previous_dashboard.goals is empty, you MUST add them to the output
   - NEVER omit goals that exist in context.goals - always include them in the goals array
   - **CRITICAL: learning_summary field** - This is the MOST IMPORTANT field for goal descriptions:
     * MUST be genuinely insightful about the user's goals, motivations, and progress
     * Use the conversation_history in context.goals[].conversation_history to understand:
       - Why the user is drawn to this goal (their stated motivations, questions, interests)
       - What they've discovered or learned so far (progress made, insights gained)
       - What purpose this goal serves for them (what they want to achieve, why it matters)
     * Also use context.exploration_conversation_summary for additional context about how the goal emerged
     * Reference specific details from conversations - what they said, what they asked, what resonated
     * Write 2-4 sentences that are specific, insightful, and grounded in actual conversation evidence
     * AVOID generic phrases like "You're drawn to this because it genuinely fascinates you" or "Exploring systems that help people build better mental models" - be specific about WHY and WHAT based on what the user actually said
     * If conversation_history is available, extract specific quotes, questions, or statements the user made about this goal
     * If no conversation_history is available, still try to infer from the goal_text itself and any themes, but be honest about limitations
     * Example good summary (when conversation history exists): "You're exploring systems that help people build better mental models because you've noticed how often people struggle with complex concepts. In your conversation, you mentioned wanting to understand the mechanisms behind effective learning tools, not just their outcomes. You're particularly interested in how these systems can bridge the gap between abstract understanding and practical application."
     * Example good summary (when no conversation history): "Based on your goal to design systems that enhance user orientation, you're focused on creating tools that help people navigate complex information spaces. The goal suggests you're interested in both the cognitive and technical aspects of how people understand and interact with complex systems."
   - If no goals in context yet, leave empty array

3. **highlights**:
   - Add only for: categorical flips (pacing/uncertainty/curiosity), confidence jumps (>= 0.2), teaching breakthroughs
   - Each needs: kind, summary, confidence, evidence_quote when available

4. **learner_model** trends:
   - Append new data points for: stability (confidence_in_profile, confidence_in_target), entry_mode, pacing_preference, uncertainty_tolerance, curiosity_type, motivation_profile
   - Each array should grow with new checkpoint data (sparse timeline)
   - If there are NO checkpoints but user_profile data exists in context, extract initial values from user_profile:
     * curiosity_type: use user_profile.curiosity_type.value if confidence > 0.5
     * pacing_preference: use user_profile.pacing_preference.value if confidence > 0.5
     * uncertainty_tolerance: use user_profile.uncertainty_tolerance.value if confidence > 0.5
     * entry_mode: use user_profile.entry_mode (people/problems/ideas values)
     * motivation_profile: use user_profile.motivation_profile values
   - Create at least one data point from user_profile if no checkpoints exist

5. **engagement**:
   - Track session_turns, last_user_msg_len trends, mode_mix when data available

User:
{user_id}

Previous dashboard JSON:
{previous_dashboard_json}

New checkpoints JSON (chronological):
{new_checkpoints_json}

Additional context (goals and themes from sessions):
{context_json}

Return the updated full dashboard JSON.


