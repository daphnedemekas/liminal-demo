[deploy]
startCommand = "if [ ! -f frontend/dist/index.html ]; then echo 'Building frontend...' && cd frontend && npx vite build && cd ..; fi && python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
